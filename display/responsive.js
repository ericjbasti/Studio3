Studio.Stage.prototype.responsive = function(){
	if(!this.hasOwnProperty("responsiveElements")) this.responsiveElements=[];
	var resize = function(){

	}

	
	Studio.DisplayObject.prototype.onResize = function(){
		if (this.rX){

		}
	}
	window.addEventListener("resize", resize, false);
}

